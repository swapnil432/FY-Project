let jwt,Admin;_8e6‍.x([["login",()=>login]]);_8e6‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_8e6‍.w("../models/admin",[["default",["Admin"],function(v){Admin=v}]]);


const JWT_SECRET = "34513HJKK34IJKBKHN9429GBJUND3JDQLL";

       const login = async (req, res) => {
  const { email, password } = req.body;
  _8e6‍.g.console.log(email, password);
  await Admin.findOne({ email }, (err, user) => {
    if (err || !user) {
      return res.status(400).json({
        error: "User not found",
      });
    }
    // const isValidPassword = user.comparePassword(password);
    // if (!isValidPassword) {
    //   return res.status(400).json({ error: "Invalid credentials" });
    // }
    // const token = jwt.sign({ _id: user._id }, JWT_SECRET);
    // res.cookie("token", token, {
    //   httpOnly: true,
    // });
    return res.json({

      message: "Welcome",
    });
  });
};
